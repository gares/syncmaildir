- if an avoidable error, but not a network one, happend, show something
- ask lucas wilcox if the osx/ Makefile template is OK
- it sould be nice to hide the Logs menu item if there are no problems,
  and maybe print in the menu item or as a tooltip the sate of the last 
  sync (requires smd-loop log parsing)
